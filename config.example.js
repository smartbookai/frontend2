// Plantilla de configuración — copia este fichero a config.js y rellena los valores reales.
// config.js está en .gitignore y NUNCA se sube al repositorio.

window.SBA_CONFIG = {
  COGNITO_REGION:    "eu-south-2",
  COGNITO_CLIENT_ID: "",   // App Client Id de Cognito (~26 chars alfanuméricos, no el User Pool Id)
  COGNITO_ENDPOINT:  "https://cognito-idp.eu-south-2.amazonaws.com/",
};
